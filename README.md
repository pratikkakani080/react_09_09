Project setup
Folder structure
Global / Local Variable and functions
Props
Navigate through route (best if two routes)
State uplifting (best if two components in same route)
Callback function (if two components in same route)
Hooks - basic functionalities
    (rules of hooks)
        - should be called under functional component only
        - should be called at the top of the component
        - hooks must be start with the keyword 'use'
        - hooks should not be called in infinite loops
        - do not define hooks inside one another

render - to show something on DOM (Document object modal)